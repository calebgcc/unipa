package manager;

import java.io.*;


interface Menu{
    public static void start(){
        File backup = new File("backup.encrypt.data");
        if(backup.exists()){
            
        }
    }
}
